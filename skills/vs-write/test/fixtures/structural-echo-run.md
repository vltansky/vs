# Deploy note

The canary shard stayed under 40 ms. Operators set REDIS_URL before the
next replica join. ECHOSKEL_CANARY_K7.

A shopping cart is an object in the system. A chat room is an object in the system.
