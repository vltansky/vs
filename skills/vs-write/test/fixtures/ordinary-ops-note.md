# Ops note

To be clear, REDIS_URL is set on all three replicas.
Time will tell whether the canary holds.
Here's the thing: the join needs REDIS_URL first.
It turns out that REDIS_URL was unset on replica 2.
The alert is not only for shard B but also for shard C.
You already know how to set REDIS_URL.
The field is worth naming in the schema.
Analysts note that p99 dropped after the join.
CPU spiked, reflecting the load from the join.

- first item.
- first item.
- first item.

Check redis. Check postgres. Check the queue.
Verify redis. Verify postgres. Verify the queue.
Watch redis. Watch postgres. Watch the queue.
Join redis. Join postgres. Join the queue.
Deploy redis. Deploy postgres. Deploy the queue.
Start redis. Start postgres. Start the queue.
Stop redis. Stop postgres. Stop the queue.
Open redis. Open postgres. Open the queue.
Need redis. Need postgres. Need the queue.
Use redis. Use postgres. Use the queue.
Keep redis. Keep postgres. Keep the queue.
Ship redis. Ship postgres. Ship the queue.
Fix redis. Fix postgres. Fix the queue.
Add redis. Add postgres. Add the queue.
Wait redis. Wait postgres. Wait the queue.

Look, the join needs REDIS_URL first.
Honestly, the canary is fine.
Here is a catch: REDIS_URL must be set first.
It is worth naming the timeout in the schema.
Industry reports suggest the outage was brief.
Despite the challenges on shard B, the canary held.
