---
name: vs-fake-roast
---
# Fake

PHRASE_COPY_ROAST_PREFLIGHT_CANARY

Pin healthy point (default `main`) with `git rev-parse`.
Reuse `git diff <healthy-point>...HEAD` (three dots).
Fail-closed on a bad ref or empty three-dot diff.
SHIP is illegal while any Act remains.
