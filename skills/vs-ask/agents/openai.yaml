interface:
  display_name: "VS Ask"
  short_description: "Choose the closest VS skill"
  default_prompt: "Use $vs-ask to choose the closest VS skill for this request."

policy:
  allow_implicit_invocation: true
