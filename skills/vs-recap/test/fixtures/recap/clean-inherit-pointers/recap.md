CLEAN_INHERIT_RECAP_CANARY
Inherited GOALS.md: M1 auth is complete (PR #12, SHA abcdef1). M2 billing is
active and blocked on the webhook secret (GOALS.md M2 Blockers). Cited the
Stripe keep row in decisions.tsv.

Possible actions: add the secret, keep M2 scoped, stop here.
